# webapp2-redis-postgres-stack-with-docker-compose
Playing around with docker compose and trying to build webapp2, redis, postgres containers that will communicate with each other in a single server.
